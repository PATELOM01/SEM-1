#include<stdio.h>
int array(int arr[int]);
int main(){
	
	int arr[n],i;
	for(i=0;i<n;i++){
		printf("enter arr[%d]",i);
		scanf("%d",&arr[i]);
		
	}	
    array(arr,n);
}
int array(int arr[int n]){
	int i,n;
	for(i=0;i<n;i++){
		printf("\nelemnt[%d] is %d",i,arr[i]);
	}
}
